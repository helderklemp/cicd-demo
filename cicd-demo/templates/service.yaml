apiVersion: v1
kind: Service
metadata:
  name: {{ include "cicd-demo.fullname" . }}
  labels:
    app: {{ include "cicd-demo.name" . }}
    chart: {{ include "cicd-demo.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "cicd-demo.name" . }}
    release: {{ .Release.Name }}
